import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.betwise.bazaar',
  appName: 'Bet Wise Bazaar',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
